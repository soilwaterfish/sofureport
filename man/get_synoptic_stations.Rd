% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stations.R
\name{get_synoptic_stations}
\alias{get_synoptic_stations}
\title{Get Station Metadata from the Synoptic API}
\usage{
get_synoptic_stations(state = NULL, network = NULL, status = "ACTIVE", ...)
}
\arguments{
\item{state}{A character string for a two-letter US state abbreviation (e.g., "UT", "CA").}

\item{network}{A character string or numeric vector of network IDs.}

\item{status}{A character string to filter by station status. "ACTIVE" is highly recommended. Defaults to "ACTIVE".}

\item{...}{Additional parameters to pass to the API. See the Synoptic "Stations Service" documentation for options like \code{radius}, \code{bbox}, \code{country}, etc.}
}
\value{
A tidy tibble of station metadata.
}
\description{
Retrieves a list of stations and their metadata (including location) from the
Synoptic (MesoWest) API. This is useful for discovering stations to use with
\code{get_synoptic_timeseries()}.
}
\examples{
\dontrun{
  # Get all active stations in Utah
  mt_stations <- get_synoptic_stations(state = "MT")

  # You can then use the STID from this data frame in other functions
  if (nrow(mt_stations) > 0) {
    a_mt_station_id <- mt_stations$STID[1]

    wx_data <- get_synoptic_timeseries(
      station_ids = a_mt_station_id,
      start_time = "2024-07-01 00:00",
      end_time = "2024-07-01 12:00",
      variables = "air_temp"
    )
  }
}
}
