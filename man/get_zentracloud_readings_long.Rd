% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/zentracloud.R
\name{get_zentracloud_readings_long}
\alias{get_zentracloud_readings_long}
\title{Get Long-Duration Timeseries Data from ZentraCloud via Chunking}
\usage{
get_zentracloud_readings_long(
  device_sn,
  start_time,
  end_time,
  token = Sys.getenv("ZENTRACLOUD_TOKEN"),
  chunk_by = "7 days",
  parallel = FALSE
)
}
\arguments{
\item{device_sn}{A character string for a single device serial number.}

\item{start_time}{The start date for the query.}

\item{end_time}{The end date for the query.}

\item{token}{Your ZentraCloud API token. Defaults to the value stored in the \code{ZENTRACLOUD_TOKEN} environment variable.}

\item{chunk_by}{A string defining the time period for each chunk (e.g., "7 days", "1 month"). Defaults to "7 days".}

\item{parallel}{A logical value. If TRUE, performs API calls in parallel. Defaults to FALSE.}
}
\value{
A tidy tibble of all successfully retrieved device readings.
}
\description{
This function acts as a wrapper around \code{zentracloud::getReadings()} to retrieve
data over long periods. It avoids API timeouts by breaking the request into
smaller, sequential time chunks.
}
\examples{
\dontrun{
  # Get a full year of data for a device, running in parallel.
  # The function will automatically use the token from your .Renviron file.
  yearly_data <- get_zentracloud_readings_long(
    device_sn = "z6-32393",
    start_time = "2023-01-01",
    end_time = "2023-12-31",
    chunk_by = "1 month",
    parallel = TRUE
  )
}
}
